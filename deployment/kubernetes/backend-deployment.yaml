apiVersion: apps/v1
kind: Deployment
metadata:
  name: heatshield-backend
  labels:
    app: heatshield-backend
spec:
  replicas: 3
  selector:
    matchLabels:
      app: heatshield-backend
  template:
    metadata:
      labels:
        app: heatshield-backend
    spec:
      containers:
      - name: backend
        image: heatshield-backend:latest
        ports:
        - containerPort: 8000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: heatshield-secrets
              key: database-url
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: heatshield-secrets
              key: secret-key
        - name: REDIS_URL
          valueFrom:
            configMapKeyRef:
              name: heatshield-config
              key: redis-url
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: heatshield-backend-service
spec:
  selector:
    app: heatshield-backend
  ports:
  - protocol: TCP
    port: 8000
    targetPort: 8000
  type: ClusterIP
